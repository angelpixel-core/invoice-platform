{
  "name": "invoice-platform",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "dev": "pnpm --parallel --filter @invoice/api --filter @invoice/web dev"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
